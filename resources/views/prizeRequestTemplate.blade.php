<a href="{{ route('admin.request.accept', $row->id) }}"
    class="btn btn-success btn-xs">Accept
</a>
<a href="{{ route('admin.request.deline', $row->id) }}"
    class="btn btn-danger btn-xs">Deline
</a>
